stages:
  - test
  - security

bandit_scan:
  stage: security
  image: python:3.11
  script:
    - pip install bandit
    - bandit -r your_project/ -f json -o bandit-report.json
  artifacts:
    when: always
    paths:
      - bandit-report.json
  